database=mysql
spring.datasource.url=jdbc:mysql://localhost:3306/infantem_db
spring.datasource.username=spring_user
spring.datasource.password=StrongPassword123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true


# Important: Currently the environments are hardcoded. 
# This can be changed, but the env vars must be real system environment variables.
# It might be better to use Docker, but we can also do it this way without Docker.
# - Javier Santos

# Uncomment the following lines to use environment variables instead of hardcoded credentials:
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:root_password}

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

infantem.jwt.secret=este_es_un_secretillo1234!!
# Token expiration time set in one dayv
infantem.jwt.timeout=86400000

