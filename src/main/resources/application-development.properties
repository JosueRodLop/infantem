database=mysql
spring.datasource.url=jdbc:mysql://localhost:3306/infantem_db
spring.datasource.username=spring_user
spring.datasource.password=StrongPassword123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

infantem.jwt.secret=este_es_un_secretillo1234!!
infantem.jwt.timeout=86400000

stripe.secret.key = ${{ secrets.CLAVE_SECRETA_STRIPE_DEV:STRIPE_SECRET_KEY }}
stripe.api.key = ${{ secrets.CLAVE_PUBLICA_STRIPE_DEV:STRIPE_API_KEY  }}
stripe.webhook.secret = ${{ secrets.CLAVE_WEBHOOK_STRIPE:STRIPE_WEBHOOK_SECRET }}
