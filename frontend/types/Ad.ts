export type Ad = {
  id: number;
  companyName: string;
  title: string;
  targetUrl: string;
  photoRoute: string;
  timeSeen: number;
  totalClicks: number;
  maxMinutes: number;
  isCompleted: boolean;
}

